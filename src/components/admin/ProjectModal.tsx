import Modal from "../layout/Modal";

const ProjectModal = () => {
    return (
        <Modal title={"AAA"} onClose={() => console.log("A")}>
            <div>AAA</div>
        </Modal>
    );
};

export default ProjectModal;
